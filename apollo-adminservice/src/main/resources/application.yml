spring:
  application:
    name: apollo-adminservice
  profiles:
    active: ${apollo_profile}

ctrip:
  appid: 100003172
  
server:
  port: 8090
  
logging:
  file: /usr/local/var/log/apollo/100003172/apollo-adminservice.log
